datasource db {
    provider = "postgresql"
    url      = "postgresql://postgres:123@localhost:5432/work_db"
}

generator client {
    provider = "prisma-client-js"
}

model User {
    id       Int    @id @default(autoincrement())
    name     String
    email    String @unique
    password String
}
